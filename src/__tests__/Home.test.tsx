// tests list
// should not render pokemon list until 2000 miliseconds timeout
// should render full pokemon list after timeout
// should filter correctly when something is typed on input

it('should...', () => true);

export {};
